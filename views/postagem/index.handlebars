<div class="card">
    <div class="card-body">
        <h1>{{postagens.titulo}}</h1>
        <hr>
        <h5>{{postagens.conteudo}}</h5>
        <hr>
        <small>Data de publicação: {{postagens.data}}</small><br>
        
    </div>
</div>

